version: '3.8'

services:
  api-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ENVIRONMENT=development
    container_name: api-server-dev

  # 선택사항: 로깅 모니터링을 위한 서비스
  # 필요시 추가 미들웨어나 의존성을 여기에 추가할 수 있습니다.
